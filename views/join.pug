extends layout

block content
  h1 Active Lobbies
  .list-group
    if lobbyList.length == 0
      a.list-group-item.list-group-item-action(href='#') No lobbies
    else
      each lobby in lobbyList
        a.list-group-item.list-group-item-action(href='/lobby/'+lobby.id)=lobby.name